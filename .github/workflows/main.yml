name: Build and Push Docker image

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Go
      uses: actions/setup-go@v3
      with:
        go-version: 1.18

    - name: Build the Go app
      run: |
        go build main.go

    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and tag the Docker Image
      run: |
        IMAGE_VERSION=$(printf "%03d" ${{ github.run_number }})
        docker build -t my-go-app:${IMAGE_VERSION} .
        docker tag my-go-app:${IMAGE_VERSION} ${{ secrets.DOCKER_USERNAME }}/my-go-app:${IMAGE_VERSION}

    - name: Push the Docker image
      run: |
        IMAGE_VERSION=$(printf "%03d" ${{ github.run_number }})
        docker push ${{ secrets.DOCKER_USERNAME }}/my-go-app:${IMAGE_VERSION}

